<?php

class FME_Canonical_Model_Observer
{
    public function saveCanonicalUrl($observer)
            {
                
                //$product = $observer->getProduct();
                //echo $observer->getData('fme_canonicalurl');
                //$att_id = $product->getData('fme_canonicalurl');
                 //$model = Mage::getModel('catalog/product')->load($product->getId());
                 //echo "<pre>"; print_r($model->getData()); exit();
                //echo "<pre>"; print_r($product->getData('fme_canonicalurl')); exit;

            }
    
    
}
